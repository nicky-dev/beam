# Orchestration: Wash — Stream Lifecycle Rethink
**Timestamp:** 2026-04-24T06:22:43Z  
**Agent:** Wash (Lead / Architect)  
**Task:** Analyze RTMP → Nostr auto-lifecycle architecture given user directive (BLS-001 rejected)  
**Status:** Complete

## Outcome

**Document:** `wash-stream-lifecycle-rethink.md`

### Key Decisions
1. **No Go Live button.** Stream lifecycle is OBS-driven (RTMP connection → auto-publish kind 30311)
2. **MVP approach:** Poll `/v1/push/list/${streamId}` every 5 seconds to detect RTMP state changes
3. **New components needed:**
   - `useStreamLifecycle()` hook — polling + state machine + Nostr event publishing
   - `StreamStatusIndicator.tsx` — visual stream state display (OFFLINE/CONNECTING/LIVE/ENDING/ENDED)
4. **Pre-flight workflow:** PresetSettings (kind 30078) is the only pre-stream config. No separate "planned" state.
5. **Existing code intact:** ForwardStreamSettings, EditStreamingInfo remain unchanged
6. **Open question for backend:** Verify `/v1/push/list` returns source RTMP connection (not just forwarding). May need new endpoint.

### Architecture Diagram
```
OBS RTMP connect
       ↓
Backend detects (RTMP server)
       ↓
Frontend polls /v1/push/list every 5s
       ↓
Detects new connection → useStreamLifecycle
       ↓
Auto-publish kind 30311 status=live to Nostr relays
       ↓
ForwardStreamSettings reacts → forwards to platforms
```

### State Machine
```
OFFLINE → (RTMP detected) → CONNECTING → (pub succeeds) → LIVE
LIVE → (RTMP disconnect) → ENDING → (update succeeds) → ENDED
```

### Implementation Phases
- **Phase 1 (Backend, Zoe):** Verify/implement RTMP status endpoint
- **Phase 2 (Frontend, Kaylee):** Implement `useStreamLifecycle()` hook + `StreamStatusIndicator` component
- **Phase 3 (QA):** Test edge cases (rapid reconnect, browser closed, multi-tab, OAuth expiry)

### Next Steps
- Await Zoe's backend verification (RTMP status endpoint)
- Kaylee blocked until backend is ready
- Wash to review implementations before merge
